## yaml-language-server: $schema=https://raw.githubusercontent.com/OAI/OpenAPI-Specification/main/schemas/v3.1/schema.yaml#/$defs/schema

# REST Schema: UserProfileParams

type: object
description: Parameters for a user's profile.
properties:
  displayName: { type: string, description: The user's display name. }
  familyName: { type: string, description: The user's family name. }
  givenName: { type: string, description: The user's given name. }
  businessName: { type: string, description: The user's business name. }
  photoUrl: { type: string, description: The user's profile picture URL. }
