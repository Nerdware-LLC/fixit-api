## yaml-language-server: $schema=https://raw.githubusercontent.com/OAI/OpenAPI-Specification/main/schemas/v3.1/schema.yaml#/$defs/schema

# REST Schema: LocalLoginCredentials

type: object
description: The user's login credentials for local authentication
properties:
  email: { $ref: "../open-api.yaml#/components/schemas/Email" }
  password: { $ref: "../open-api.yaml#/components/schemas/Password" }
required:
  - email
  - password
