## yaml-language-server: $schema=https://raw.githubusercontent.com/OAI/OpenAPI-Specification/main/schemas/v3.1/schema.yaml#/$defs/request-body

# req.body for /subscriptions/check-promo-code

required: true
content:
  application/json:
    schema:
      type: object
      properties:
        promoCode: { $ref: "../open-api.yaml#/components/schemas/PromoCode" }
      required:
        - promoCode
