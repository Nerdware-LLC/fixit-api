## yaml-language-server: $schema=https://raw.githubusercontent.com/OAI/OpenAPI-Specification/main/schemas/v3.1/schema.yaml#/$defs/schema

# REST Schema: PreFetchedUserItemsResponseField

type: object
description: |
  An object which contains pre-fetched user items under the key `userItems`.
properties:
  userItems:
    $ref: "../open-api.yaml#/components/schemas/PreFetchedUserItems"
required:
  - userItems
