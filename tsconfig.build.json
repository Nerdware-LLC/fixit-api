{
  "extends": "./tsconfig.json",
  // Exclude test files from build:
  "exclude": [
    "**/*.test.ts",
    "**/*.test.js",
    "**/*.spec.ts",
    "**/*.spec.js",
    "**/__tests__/",
    "**/__mocks__/"
  ]
}
