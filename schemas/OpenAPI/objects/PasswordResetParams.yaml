## yaml-language-server: $schema=https://raw.githubusercontent.com/OAI/OpenAPI-Specification/main/schemas/v3.1/schema.yaml#/$defs/schema

# REST Schema: PasswordResetParams

type: object
description: A new password and a valid password reset token
properties:
  password:
    $ref: "../open-api.yaml#/components/schemas/Password"
  passwordResetToken:
    $ref: "../open-api.yaml#/components/schemas/PasswordResetToken"
required:
  - password
  - passwordResetToken
